{
  "name": "nodejs-mtproxy",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Node.js Telegram MTProxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate-secret": "node server.js --generate-secret"
  },
  "engines": {
    "node": ">=20"
  }
}
